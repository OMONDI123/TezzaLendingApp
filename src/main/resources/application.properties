# ============================================
# JWT TOKEN CONFIGURATION PROPERTIES
# ============================================
bezkoder.app.jwtSecret=${JWT_SECRET}
bezkoder.app.jwtExpirationMs=${JWT_EXPIRATION}

# ============================================
# DATABASE CONFIGURATIONS
# ============================================
#server.port=${SERVER_PORT}
#server.servlet.context-path=${CONTEXT_PATH}
#spring.datasource.url=${SPRING_DATASOURCE_URL}
#spring.datasource.username=${SPRING_DATASOURCE_USERNAME}
#spring.datasource.password=${SPRING_DATASOURCE_PASSWORD}
#spring.jpa.properties.hibernate.dialect=${HIBERNATE_DIALECT}
#spring.jpa.properties.hibernate.jdbc.lob.non_contextual_creation=${HIBERNATE_LOB_NON_CONTEXTUAL}




#Smart Debt App Connections
server.port=8083
server.servlet.context-path=/tezza
spring.datasource.url = jdbc:postgresql://localhost:5432/tezzaapp
spring.datasource.username = tezzauser
spring.datasource.password = tezza#$2026test
pring.jpa.properties.hibernate.dialect =  org.hibernate.dialect.PostgreSQL95Dialect
spring.jpa.properties.hibernate.jdbc.lob.non_contextual_creation=true





# ============================================
# HIBERNATE PROPERTIES
# ============================================
spring.jpa.hibernate.ddl-auto=${HIBERNATE_DDL_AUTO}
spring.jpa.properties.hibernate.format_sql=${HIBERNATE_FORMAT_SQL}

# ============================================
# FILE & DOCUMENT CONFIGURATIONS
# ============================================
documentsUploadDir=${DOCUMENTS_UPLOAD_DIR}
filePath=${FILE_PATH}

# ============================================
# BIG BLUE BUTTON CONFIGURATIONS
# ============================================
bbb.api.url=${BBB_API_URL}
bbb.api.secret=${BBB_API_SECRET}

# ============================================
# SPRING & DOMAIN CONFIGURATIONS
# ============================================
spring.mvc.pathmatch.matching-strategy=${SPRING_MVC_PATHMATCH}
domain.url=${DOMAIN_URL}

# ============================================
# SCHEDULER CONFIGURATIONS
# ============================================
scheduler.loan.process.delay=${SCHEDULER_LOAN_DELAY}
scheduler.loan.pageSize=${SCHEDULER_LOAN_PAGESIZE}
scheduler.loan.calculatorReturnsDelta=${SCHEDULER_LOAN_DELTA}

# ============================================
# PAYPAL CONFIGURATIONS
# ============================================
paypal.mode=${PAYPAL_MODE}
paypal.client.app=${PAYPAL_CLIENT_APP}
paypal.client.secret=${PAYPAL_CLIENT_SECRET}

# ============================================
# STRIPE CONFIGURATIONS
# ============================================
stripe.key=${STRIPE_KEY}

# ============================================
# CHECKOUT.COM CONFIGURATIONS
# ============================================
checkout.api.public-key=${CHECKOUT_PUBLIC_KEY}
checkout.api.secret-key=${CHECKOUT_SECRET_KEY}
checkout.access.key=${CHECKOUT_ACCESS_KEY}
checkout.access.key.secrete=${CHECKOUT_ACCESS_SECRET}

# ============================================
# SERVER CONFIGURATIONS
# ============================================
server.max-http-header-size=${SERVER_MAX_HEADER_SIZE}

# ============================================
# GOOGLE DRIVE CONFIGURATIONS
# ============================================
google.drive.credentials.file.path=${GOOGLE_DRIVE_CREDENTIALS}
google.drive.folder.name=${GOOGLE_DRIVE_FOLDER}